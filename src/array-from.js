// ==========================================================================
// array-from
// ==========================================================================

const products = 
  Array.from(document.querySelectorAll(".product__item"));

console.log(products);
